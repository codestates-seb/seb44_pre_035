spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

  profiles:
    include: jwt
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: true

